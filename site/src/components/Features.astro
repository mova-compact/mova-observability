---
const features = [
  {
    icon: "&#x1F4E6;",
    title: "Compact Run Artifact",
    desc: "Collect semantic events directly into compact run data with strict validation.",
  },
  {
    icon: "&#x1F5C2;",
    title: "Expanded JSONL",
    desc: "Expand compact payload back to readable JSONL for audits and debugging.",
  },
  {
    icon: "&#x1F517;",
    title: "Semantic + Time Join",
    desc: "Join semantic steps with OTel timeline using deterministic run-level correlation.",
  },
  {
    icon: "&#x1F6E1;",
    title: "Verification Gates",
    desc: "Run integrity and consistency checks, then inspect summary and sanity control outputs.",
  },
];
---

<section id="features" class="section">
  <div class="container">
    <h2 class="section-title">What You Get</h2>
    <p class="section-subtitle">
      Four concrete outputs you can use on day one.
    </p>
    <div class="grid-2 features-grid">
      {features.map((f) => (
        <div class="feature-card">
          <span class="feature-icon" set:html={f.icon} />
          <h3 class="feature-title">{f.title}</h3>
          <p class="feature-desc">{f.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .features-grid {
    border-top: 1px solid var(--border);
    border-left: 1px solid var(--border);
    gap: 0;
  }

  .feature-card {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 2rem;
    border-right: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    aspect-ratio: auto;
  }

  @media (min-width: 640px) {
    .feature-card {
      aspect-ratio: auto;
      justify-content: center;
    }
  }

  .feature-icon {
    font-size: 1.5rem;
    line-height: 1;
  }

  .feature-title {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--text);
  }

  .feature-desc {
    color: var(--text-secondary);
    font-size: 0.875rem;
    line-height: 1.6;
  }
</style>
