---
const steps = [
  {
    num: "01",
    title: "Collect",
    desc: "Pipe semantic events from your running agent directly into compact binary. Time-axis data comes from OTel or any structured JSONL source.",
    cmd: "python my_agent.py | mova-compact collect --run-dir runs/demo --run-id demo --input -",
  },
  {
    num: "02",
    title: "Join",
    desc: "Deterministically correlate time and semantic streams by run_id and step_id. Get confidence scores per match.",
    cmd: "mova-compact join --run-dir runs/demo --time otel.jsonl",
  },
  {
    num: "03",
    title: "Verify",
    desc: "Run built-in integrity checks, negative controls (shift/shuffle), and determinism proof. Evidence-grade output.",
    cmd: "mova-compact verify --run-dir runs/demo",
  },
];
---

<section id="how-it-works" class="section">
  <div class="container">
    <h2 class="section-title">How It Works</h2>
    <p class="section-subtitle">
      Collect, join, verify. From raw logs to evidence in minutes.
    </p>
    <div class="steps">
      {steps.map((s) => (
        <div class="step">
          <div class="step-header">
            <span class="step-num">{s.num}</span>
            <h3 class="step-title">{s.title}</h3>
          </div>
          <p class="step-desc">{s.desc}</p>
          <pre><code>{s.cmd}</code></pre>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .steps {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .step {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 0;
    padding: 2rem;
  }

  .step-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 0.75rem;
  }

  .step-num {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 700;
    color: #ffffff;
    background: var(--accent);
    width: 2rem;
    height: 2rem;
    border-radius: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .step-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text);
  }

  .step-desc {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .step pre {
    margin: 0;
  }
</style>
