---
---

<section id="quickstart" class="section">
  <div class="container">
    <h2 class="section-title">Quickstart</h2>
    <p class="section-subtitle">
      One canonical flow. Copy, run, inspect outputs.
    </p>

    <div class="card quickstart-card">
      <pre><code>{`# 1) Download binary + connectors
curl -fsSL https://github.com/mova-compact/mova-downloads/releases/latest/download/mova-compact-linux -o mova-compact
chmod +x mova-compact
curl -fsSL https://github.com/mova-compact/mova-downloads/releases/latest/download/mova-connectors-python.zip -o mova-connectors-python.zip
unzip mova-connectors-python.zip

# 2) Sanity
./mova-compact -h

# 3) Collect semantic events
python connectors/openai/openai_connector_demo.py | ./mova-compact collect --run-dir runs/demo --run-id demo-001 --input - --strict

# 4) Expand
./mova-compact expand --run-dir runs/demo

# 5) Join with OTel timeline
./mova-compact join --run-dir runs/demo --time otel_time.jsonl

# 6) Verify
./mova-compact verify --run-dir runs/demo

# 7) Inspect outputs
cat runs/demo/outputs/summary.json
cat runs/demo/outputs/final_observation.jsonl | head -3`}</code></pre>

      <div class="output-preview">
        <span class="output-label">Run With Codex</span>
        <pre><code>{`codex exec "Run MOVA quickstart end-to-end and report:
1) verify status
2) coverage from outputs/summary.json
3) gate_passed from outputs/sanity_controls.json
4) any command mismatch in the docs."`}</code></pre>
      </div>
    </div>
  </div>
</section>

<style>
  .quickstart-card {
    max-width: 720px;
  }

  .output-preview {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
  }

  .output-label {
    display: block;
    font-size: 0.8rem;
    color: var(--text-secondary);
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
</style>
