---
---

<section id="quickstart" class="section">
  <div class="container">
    <h2 class="section-title">Quick Start (Sequential)</h2>
    <p class="section-subtitle">
      Codex-first runbook: sanity check, collect, expand, OTel join, verify, and PASS criteria.
    </p>

    <div class="card quickstart-card">
      <pre><code>{`# 1) Quick sanity (binary is callable)
mova-compact -h
mova-compact collect -h

# 2) Codex/OpenAI demo pipeline (copy-paste ready)
python connectors/openai/openai_connector_demo.py \
  | mova-compact collect --run-dir runs/o1 --run-id o1 --input - --strict
mova-compact expand --run-dir runs/o1

# 3) OTel time-axis export
python export_otel_jsonl.py --service my-agent --out otel_time.jsonl

# 4) Join + verify
mova-compact join --run-dir runs/o1 --time otel_time.jsonl
mova-compact verify --run-dir runs/o1

# 5) Read actual outputs
cat runs/o1/outputs/final_observation.jsonl | head -3
cat runs/o1/outputs/summary.json
cat runs/o1/outputs/sanity_controls.json`}</code></pre>

      <div class="output-preview">
        <span class="output-label">PASS Criteria</span>
        <pre><code>{`verify -> status: ok
outputs/summary.json:
  coverage ~= 1.0
  unmatched_semantic = 0
  unmatched_time = 0
outputs/sanity_controls.json:
  gate_passed = true`}</code></pre>
      </div>

      <div class="output-preview">
        <span class="output-label">Codex One-Shot Task</span>
        <pre><code>{`codex exec "Read this landing runbook, run MOVA quickstart end-to-end in /tmp/mova_eval, and report:
1) verify status
2) coverage from outputs/summary.json
3) gate_passed from outputs/sanity_controls.json
4) any command mismatch in the docs."`}</code></pre>
      </div>
    </div>
  </div>
</section>

<style>
  .quickstart-card {
    max-width: 720px;
  }

  .output-preview {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
  }

  .output-label {
    display: block;
    font-size: 0.8rem;
    color: var(--text-secondary);
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
</style>
