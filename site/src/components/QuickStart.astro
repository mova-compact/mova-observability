---
---

<section id="quickstart" class="section">
  <div class="container">
    <h2 class="section-title">Quick Start (Sequential)</h2>
    <p class="section-subtitle">
      End-to-end order: collect semantic axis from agent, add OTel time axis, join, verify, inspect.
    </p>

    <div class="card quickstart-card">
      <pre><code>{`# 1) Collect semantic axis from your agent
# Option A: Claude / Anthropic connector
python connectors/anthropic/anthropic_connector_demo.py \
  | mova-compact collect --run-dir runs/my-run --run-id my-run-001 --input -
# Option B: Codex / OpenAI connector
# python connectors/openai/openai_connector_demo.py \
#   | mova-compact collect --run-dir runs/my-run --run-id my-run-001 --input -

# 2) Expand compact payload to JSONL
mova-compact expand --run-dir runs/my-run

# 3) Export OTel time-axis JSONL (from your telemetry pipeline)
python export_otel_jsonl.py --service my-agent --out otel_time.jsonl

# 4) Join semantic + time axes
mova-compact join --run-dir runs/my-run --time otel_time.jsonl

# 5) Verify determinism and integrity gates
mova-compact verify --run-dir runs/my-run

# 6) Inspect correlated evidence
cat runs/my-run/reports/merged_journal.jsonl | head -3`}</code></pre>

      <div class="output-preview">
        <span class="output-label">Sample output (merged_journal.jsonl)</span>
        <pre><code>{`{"type":"match","event_id":"E000001","step_id":"E000001",
 "confidence":0.93,"ambiguity_flag":false,
 "event":{"ts_start":2.0,"ts_end":2.001,"tool_name":"file_history"},
 "semantic":{"run_id":"my-run-001","tool_name":"file_history",
  "decision_kind":"file-history-snapshot"}}`}</code></pre>
      </div>
    </div>
  </div>
</section>

<style>
  .quickstart-card {
    max-width: 720px;
  }

  .output-preview {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
  }

  .output-label {
    display: block;
    font-size: 0.8rem;
    color: var(--text-secondary);
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
</style>
