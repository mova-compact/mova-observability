---
---

<section id="integrations" class="section">
  <div class="container">
    <h2 class="section-title">Integrations</h2>
    <p class="section-subtitle">
      Supported connector paths with the same collector contract.
    </p>

    <div class="grid-2 integration-grid">
      <div class="card integration-card">
        <h3 class="integration-title">Codex / OpenAI</h3>
        <p class="integration-desc">
          Use the OpenAI adapter to emit collector-ready semantic events.
        </p>
        <pre><code>{`python connectors/openai/openai_connector_demo.py | ./mova-compact collect --run-dir runs/demo --run-id demo-001 --input - --strict`}</code></pre>
      </div>

      <div class="card integration-card">
        <h3 class="integration-title">Anthropic</h3>
        <p class="integration-desc">
          Use the Anthropic adapter to emit the same semantic contract.
        </p>
        <pre><code>{`python connectors/anthropic/anthropic_connector_demo.py | ./mova-compact collect --run-dir runs/demo --run-id demo-001 --input - --strict`}</code></pre>
      </div>
    </div>

    <div class="card integration-card integration-wrap">
      <h3 class="integration-title">Wrap Your Tools</h3>
      <p class="integration-desc">
        Minimal wrapper snippet to emit consistent tool events.
      </p>
      <pre><code>{`from connectors.openai.openai_connector import OpenAIConnector

connector = OpenAIConnector(run_id="demo-001")
safe_tool = connector.wrap_tool("my_tool", my_tool)
result = safe_tool("arg")
connector.emit_model_output(note="step_done", result_kind="ok")`}</code></pre>
    </div>
  </div>
</section>

<style>
  .integration-grid {
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .integration-card {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
    min-width: 0;
  }

  .integration-title {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--text);
  }

  .integration-desc {
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  .integration-card pre {
    margin: 0;
  }

  .integration-wrap {
    margin-top: 0;
  }
</style>
